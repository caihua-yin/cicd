- hosts: yinc2-cluster
  remote_user: vagrant
  become: true
  vars:
    - registrator_name: registrator-consul
    - registrator_protocol: consul
    - registrator_port: 8500
  roles:
    - common
    - docker
    - app
    - consul
    - registrator
    - nginx
    - consul-template
