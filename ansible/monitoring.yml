# Monitoring And Alerting
- hosts: influxdb
  remote_user: vagrant
  become: true
  roles:
    - common
    - docker
    - influxdb

# FIXME
- hosts: collectd
  remote_user: vagrant
  become: true
  roles:
    - common
    - docker
    - collectd

- hosts: statsd
  remote_user: vagrant
  become: true
  roles:
    - common
    - docker
    - statsd

- hosts: grafana
  remote_user: vagrant
  become: true
  roles:
    - common
    - docker
    - grafana

